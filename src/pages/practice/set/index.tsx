export default function PracticeSet() {
    // this will be the most expansive component with a check button for each song that logs the repetition
    // and crosses it off the setlist (doesn't remove it, just exes it or something like that)
    return (
        <div style={{margin: 'auto'}}>
            <h1 style={{color: 'white', textAlign: 'center'}}>Practice Set</h1>
        </div>
    )
}